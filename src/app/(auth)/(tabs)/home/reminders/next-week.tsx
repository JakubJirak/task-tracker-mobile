import { Text, View } from "react-native";

export default function RemindersNextWeek() {
  return (
    <View className="bg-primary relative flex-1 px-3">
      <Text className="text-text">reminders - next-week</Text>
    </View>
  );
}
